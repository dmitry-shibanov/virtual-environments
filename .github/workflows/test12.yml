name: pr review fork
on:
  pull_request_review:
    types: [submitted]
jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "pull request review"
